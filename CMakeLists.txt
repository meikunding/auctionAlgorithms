CMAKE_MINIMUM_REQUIRED(VERSION 3.6)

project(auctionAlgorithms)

set(CMAKE_BINARY_DIR ${CMAKE_SOURCE_DIR}/bin)
set(EXECUTABLE_OUTPUT_PATH ${CMAKE_BINARY_DIR})
set(LIBRARY_OUTPUT_PATH ${CMAKE_SOURCE_DIR}/lib)

subdirs(auction)
subdirs(test)
